services:
  traefik:
    image: traefik:v3.7
    container_name: traefik
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - local_gateway

networks:
  local_gateway:
    external: true
